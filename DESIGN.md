# Design

<!-- 视觉方向锁定：极简护眼阅读（参考 czhlove.cn）+ 现代 UI 质感升级 -->

## 视觉世界

在 czhlove.cn 的信息架构与克制感之上，用现代 UI 语言重做质感：**纸感极简 × 精致细节**。结构照搬参考站（站点简介 + 横排标签筛选 + 瀑布流 + 最新/精选切换 + 加载更多），视觉升级为现代层次（微妙的 surface 分层、适度圆角、克制的阴影、精心的字体层级、平滑微交互）。

绝不廉价：拒绝默认模板感（大渐变、玻璃拟态滥用、霓虹、发光按钮）；每个状态都有设计（hover、空态、加载、选中）。

## 设计原则

1. **内容即界面**：排版是主角，装饰让位；阅读体验优先。
2. **克制的层次**：不纯平、不浮夸——用极浅 surface 分层 + 细边框 + 极轻阴影建立层级。
3. **三主题一等公民**：light / dark / sepia 三套 token 同样精致，跟随系统 + 手动记忆。
4. **细节定品质**：间距走 8pt 网格，字号层级明确，hover 过渡统一 150-250ms。
5. **性能即体验**：图片懒加载 + blur 占位、字体用系统栈、动效只用在有意义处。

## 主题系统

三主题基础色（用户指定）：

| Token | light | dark | sepia |
|---|---|---|---|
| `--bg` | rgb(228,228,231) | rgb(39,39,42) | rgb(214,209,194) |
| `--fg` | rgb(39,39,42) | rgb(228,228,231) | rgb(39,39,42) |

派生色（现代层次）：

| Token | light | dark | sepia |
|---|---|---|---|
| `--surface` | rgba(255,255,255,.55) | #323237 | rgba(255,255,255,.35) |
| `--surface-strong` | #FBFBFC | #3A3A40 | #E6E1D2 |
| `--border` | rgba(0,0,0,.09) | rgba(255,255,255,.12) | rgba(0,0,0,.10) |
| `--fg-muted` | rgba(39,39,42,.58) | rgba(228,228,231,.58) | rgba(39,39,42,.58) |
| `--fg-faint` | rgba(39,39,42,.38) | rgba(228,228,231,.38) | rgba(39,39,42,.38) |
| `--selection` | rgba(39,39,42,.12) | rgba(228,228,231,.16) | rgba(39,39,42,.12) |

语义色：`--link`（文章内链）、`--code-bg`、`--code-fg` 由主题派生；`--accent` = #2F81F7（dark 主题微调亮一档）。

## 字体排版

- 字体栈（现代感 + 零加载成本，系统栈优先）：
  - `--font-sans`: `system-ui, -apple-system, "PingFang SC", "HarmonyOS Sans SC", "MiSans", "Microsoft YaHei", sans-serif`
  - `--font-mono`: `ui-monospace, "JetBrains Mono", "SF Mono", Menlo, Consolas, monospace`
- 层级：
  - 站点名 20/600；文章标题 24-28/600/1.3；卡片标题 16-18/600/1.4
  - 正文 16/1.8（文章页阅读宽度 680-720px）
  - 元数据（日期/阅读时长/标签）13 mono / fg-muted
  - 标签 pill 13/500
- 中英文混排：中文不斜体；代码与元数据用 mono。

## 布局与栅格

- 全局：8pt 间距网格；最大内容宽度 1120px，两侧 padding 20-24px。
- 顶部导航：sticky + `backdrop-blur` 毛玻璃，四项菜单（首页/相册/关于/友链）+ 主题切换按钮（三态循环或下拉）。
- 首页：站点简介一句话 → 筛选条（sticky，横排标签多选 + 最新/精选 toggle，**筛选不改 URL**）→ 瀑布流文章卡 → 无限滚动。
- 瀑布流列数：≥1024px 三列；640-1023px 两列；<640px 两列（卡片瘦身）。
- 卡片结构：封面（可选，懒加载 + blur 占位）→ 日期（mono 小字）→ 标题 → 摘要（两行截断）→ 底部标签行。
- 相册页：同瀑布流语法，图片主导；筛选条隐藏（点击标签图标展开）；支持精选。
- 文章页：元数据头（日期 · 阅读时长 · 标签）+ 标题 + MDX 内容 + giscus 评论；TOC 可选。
- 关于/友链：内容页；友链卡片 = 站点名 + 链接 + 一句话标签。

## 组件规范

- **Nav**：sticky、毛玻璃、内容左侧、主题切换右侧；移动端菜单折叠。
- **TagPill**：多选组合；未选 = 边框，选中 = 主题色填充；hover 过渡 150ms。
- **Toggle（最新/精选）**：分段控件，选中态明确。
- **PostCard**：`--surface` 底 + 1px `--border` + 圆角 12px + hover `translateY(-2px)` + 边框加深 + 阴影加深（150ms）。
- **Button**：主按钮 = 主题色填充圆角 10px；次按钮 = 边框。
- **代码块**：Shiki 渲染、圆角 10px、`--code-bg`、复制按钮、横向滚动。
- **图片**：`loading="lazy"` + 宽高占位（防 CLS）+ 加载时 shimmer。
- **空态/加载态**：空筛选结果显示"没有匹配的文章"；无限滚动底部 spinner。

## 交互与动效

- 主题切换：颜色 250ms 过渡，无闪烁（首屏内联脚本防 FOUC）。
- 筛选：结果区交叉淡入 200ms；标签选中立即生效（纯客户端）。
- 无限滚动：IntersectionObserver 触底加载，每次 9-12 条。
- 阅读进度条（文章页，可选）：顶部 2px 进度。
- 无侵入动效：hover/active/焦点态统一，尊重 `prefers-reduced-motion`。

## 响应式

- 桌面（≥1024）：三列瀑布流，筛选条常驻。
- 平板（640-1023）：两列，筛选条横向滚动。
- 手机（<640）：两列窄卡，导航折叠，筛选条横向滚动，点击区域 ≥44px。
- 触控：hover 效果在触屏降级为无。

## 与参考站的关系

- **继承**：信息架构（简介 + 横排标签 + 瀑布流 + 最新/精选 + 加载更多）、筛选不改 URL、三主题、移动端适配、整体克制。
- **升级**（现代 UI 质感）：surface 分层与圆角、微阴影、字体层级与间距节奏、毛玻璃导航、统一微交互、Shiki 代码块、图片占位与懒加载、空态/加载态。
- **不复制**：不逐像素照抄参考站的字体、间距与装饰细节——保留结构，重做质感。

## 已确认决策（2026-09-10 锁定，后期可调）

1. **强调色**：引入克制的 accent `#2F81F7`（青蓝），仅用于链接、标签选中态、精选徽章；三主题下保持对比度（dark 主题可微调亮一档）。
2. **字体**：系统栈（中文无衬线系统字体 + mono 代码字体），不引入 web font，零加载成本。
3. **圆角**：卡片 12px；按钮 10px；代码块 10px。
4. **卡片封面**：发布时可配（可选）；开启时统一 16:9 比例 + 懒加载 + blur 占位，避免参差。
